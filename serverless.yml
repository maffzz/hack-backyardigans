org: martinbonilla
service: alertautec-backend

provider:
  name: aws
  runtime: python3.13
  memorySize: 1024
  timeout: 30
  stage: dev
  region: us-east-1
  environment:
    WEBSOCKET_SERVICE_NAME: websocket-backend
    STAGE: ${self:provider.stage}
    AWS_REGION: ${self:provider.region}
  iam:
    role: arn:aws:iam::492147277453:role/LabRole
  iamRoleStatements:
    - Effect: Allow
      Action:
        - lambda:InvokeFunction
      Resource:
        - arn:aws:lambda:${self:provider.region}:*:function:websocket-backend-${self:provider.stage}-*

functions:

  createIncident:
    handler: student/createIncident.handler
    events:
      - http:
          path: student/incidents
          method: post
          cors: true

  listIncidents:
    handler: student/listIncidents.handler
    events:
      - http:
          path: student/incidents
          method: get
          cors: true

  listMine:
    handler: student/listMine.handler
    events:
      - http:
          path: student/incidents/mine
          method: get
          cors: true

  getIncident:
    handler: student/getIncident.handler
    events:
      - http:
          path: student/incidents/{id}
          method: get
          cors: true

  uploadAttachment:
    handler: student/uploadAttachment.handler
    events:
      - http:
          path: student/incidents/{id}/attachments
          method: post
          cors: true

  validateLocation:
    handler: student/validateLocation.handler
    events:
      - http:
          path: student/incidents/validate-location
          method: post
          cors: true

  previewIncident:
    handler: student/previewIncident.handler
    events:
      - http:
          path: student/incidents/preview
          method: post
          cors: true

  statsBasic:
    handler: student/statsBasic.handler
    events:
      - http:
          path: student/incidents/stats/basic
          method: get
          cors: true



  updateStatus:
    handler: staff/updateStatus.handler
    events:
      - http:
          path: staff/incidents/{id}/status
          method: put
          cors: true

  addComment:
    handler: staff/addComment.handler
    events:
      - http:
          path: staff/incidents/{id}/comment
          method: post
          cors: true

  assignDepartment:
    handler: staff/assignDepartment.handler
    events:
      - http:
          path: staff/incidents/{id}/assign
          method: post
          cors: true

  listForDepartment:
    handler: staff/listForDepartment.handler
    events:
      - http:
          path: staff/incidents/by-department
          method: get
          cors: true

  getIncidentEvents:
    handler: staff/getIncidentEvents.handler
    events:
      - http:
          path: staff/incidents/{id}/events
          method: get
          cors: true

  staffStats:
    handler: staff/staffStats.handler
    events:
      - http:
          path: staff/incidents/stats
          method: get
          cors: true

resources:
  Resources:

    Incidentes:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: Incidentes
        AttributeDefinitions:
          - AttributeName: incident_id
            AttributeType: S
        KeySchema:
          - AttributeName: incident_id
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST

    IncidenteEventos:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: IncidenteEventos
        AttributeDefinitions:
          - AttributeName: incident_id
            AttributeType: S
          - AttributeName: timestamp
            AttributeType: S
        KeySchema:
          - AttributeName: incident_id
            KeyType: HASH
          - AttributeName: timestamp
            KeyType: RANGE
        BillingMode: PAY_PER_REQUEST


    Departamentos:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: Departamentos
        AttributeDefinitions:
          - AttributeName: department_id
            AttributeType: S
        KeySchema:
          - AttributeName: department_id
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST

    CognitoUserPool:
      Type: AWS::Cognito::UserPool
      Properties:
        UserPoolName: AlertaUTECUserPool
        UsernameAttributes:
          - email
        AutoVerifiedAttributes:
          - email
        MfaConfiguration: "OFF"
        Schema:
          - Name: email
            Required: true
            Mutable: false
          - Name: department
            AttributeDataType: "String"
            Mutable: true
            Required: false
          - Name: role
            AttributeDataType: "String"
            Mutable: true
            Required: false
        Policies:
          PasswordPolicy:
            MinimumLength: 8
            RequireUppercase: false
            RequireLowercase: true
            RequireNumbers: true
            RequireSymbols: false

    CognitoUserPoolClient:
      Type: AWS::Cognito::UserPoolClient
      Properties:
        ClientName: alertaUTEC-app-client
        GenerateSecret: false
        UserPoolId:
          Ref: CognitoUserPool
        ExplicitAuthFlows:
          - ALLOW_REFRESH_TOKEN_AUTH
          - ALLOW_USER_PASSWORD_AUTH
          - ALLOW_USER_SRP_AUTH
        PreventUserExistenceErrors: ENABLED

    StudentGroup:
      Type: AWS::Cognito::UserPoolGroup
      Properties:
        GroupName: STUDENT
        UserPoolId:
          Ref: CognitoUserPool

    StaffGroup:
      Type: AWS::Cognito::UserPoolGroup
      Properties:
        GroupName: STAFF
        UserPoolId:
          Ref: CognitoUserPool

    AdminGroup:
      Type: AWS::Cognito::UserPoolGroup
      Properties:
        GroupName: ADMIN
        UserPoolId:
          Ref: CognitoUserPool

